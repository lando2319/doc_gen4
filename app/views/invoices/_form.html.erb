<%= form_for(@invoice) do |invoice| %>

 <%= invoice.collection_select(:person_id, Person.all, :id, :name) %>


<%= invoice.label :expense %>
<%= invoice.text_field :expense %>





<%= invoice.submit %>
<% end %>
